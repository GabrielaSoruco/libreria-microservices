spring:
  jpa:
    show-sql: true
    hibernate.ddl-auto: update
  application:
    name: laptop-service

server:
  port: 8082

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
      fetch-registry: true
      register-with-eureka: true
